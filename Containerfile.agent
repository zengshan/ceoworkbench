FROM node:22-bookworm-slim

WORKDIR /app

ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci

COPY packages ./packages

ENTRYPOINT ["npx", "tsx", "/app/packages/agent-runner/src/bin.ts"]
