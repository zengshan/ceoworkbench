FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY packages ./packages

ENV NODE_ENV=production

ENTRYPOINT ["/app/node_modules/.bin/tsx", "/app/packages/agent-runner/src/bin.ts"]
